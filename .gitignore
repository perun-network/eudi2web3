/target
/circuits/tests/node_modules

# In the long run we almost certainly want this in the repo.
# But it doesn't make much sense while the circuits keep changing.
/zkey
/ptau

# Key material
*.vk
*.sk
me.addr
*.pem

# useful for debugging
input.json
constraint_counts.txt

